import styled from 'styled-components';

export const HeaderContainer = styled.header`
  display: flex;
  align-items: center;
  justify-content: space-between;
  background: #0c233a;
  background: linear-gradient(0deg, rgba(12, 35, 58, 1) 50%, rgba(11, 27, 43, 1) 100%);

  a {
    margin: 4rem 0 8.375rem;
  }
`;
